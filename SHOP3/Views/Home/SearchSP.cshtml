@model IEnumerable<SHOP3.Models.HangHoa>

@{
    ViewData["Title"] = "Kết Quả Tìm Kiếm";
}
<section id="aa-product-category">
    <div class="container">
        <div class="row">
            <div class="col-lg-9 col-md-9 col-sm-8 col-md-push-3">
                <div class="aa-product-catg-content">
                    <div class="aa-product-catg-body">
                        <ul class="aa-product-catg">
                            @{
                                foreach (var item in Model)
                                {
                                    <li>
                                        <figure>
                                            <a class="aa-product-img" asp-action="Details" asp-controller="HangHoas" asp-route-id="@item.Url"><img src="~/img/@item.Hinh" alt="polo shirt img"></a>
                                            <a class="aa-add-card-btn" href="#"><span class="fa fa-shopping-cart"></span>Thêm vào giỏ</a>
                                            <figcaption>
                                                <h4 class="aa-product-title"><a asp-action="Details" asp-controller="HangHoas" asp-route-id="@item.Url">@item.TenHh</a></h4>
                                                <span class="aa-product-price">@String.Format("{0:0,0 đ}", item.DonGia)</span>
                                                <p class="aa-product-descrip"> @item.MoTa</p>
                                            </figcaption>
                                        </figure>

                                        <!-- product badge -->
                                        <span class="aa-badge aa-sold-out" href="#">NEW!</span>
                                    </li>
                                }
                            }


                        </ul>

                    </div>

                </div>
            </div>
            <div class="col-lg-3 col-md-3 col-sm-4 col-md-pull-9">
                <aside class="aa-sidebar">
                    <!-- single sidebar -->
                    <div class="aa-sidebar-widget">
                        <h3>Kết Quả Tìm Kiếm:</h3>

                    </div>



                </aside>
            </div>

        </div>
    </div>
</section>